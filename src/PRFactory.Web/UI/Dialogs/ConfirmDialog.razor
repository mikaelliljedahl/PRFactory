@using Radzen
@using Radzen.Blazor

<!-- This is a static helper component for showing confirmation dialogs -->
<!-- It doesn't render anything - it's just a container for the dialog service methods -->

@code {
    // This component doesn't render anything
    // It's designed to be used via RadzenDialog service injection
}
